bench_version: "1.0"
id: "dilithium2-baseline"
protocol:
  name: "dilithium2"
observation:
  map: "bytes"
  note: "Dilithium2 signature bytes projection"

n_values: [8, 16, 32, 64, 128, 256]

rng_families:
  - name: reference
  - name: bounded_interval
  - name: grid_comb
  - name: recurrent_low_rank

usage:
  fixed_key: true
  message_model: "counter"

trials: 24
repeats: 3

metrics:
  fpr_target: 0.05

output:
  base_dir: "benchmarks/runs"
  save_curves: true
  save_datasets: false

reproducibility:
  seed: 7
  deterministic: true
